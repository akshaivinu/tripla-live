Run the project locally in a few steps:

1. Install dependencies
npm install
2. Start the development server
npm run dev
3. Open in browser

Visit: http://localhost:3000

Assets & Media

This experience is media-driven. The quality of assets directly defines the quality of the product.

Replace placeholder assets in:

/public/assets

Use the following structure:

hero-video.mp4 → Cinematic intro (highest impact asset)
the-avenue.png → Luxury retail environment
nickelodeon-universe.png → Theme park energy
dreamworks-waterpark.png → Scale + activity
big-snow.png → Unique differentiator
events-platform.png → Live events / activations
Guidelines
Prefer real property images over generated visuals
Use short, loopable videos (4–8s)
Maintain consistent color grading across all assets
Avoid clutter — each asset should feel intentional and cinematic
Architecture Overview

The platform is built as a modular, media-driven system, not a static page.

Core Stack
Next.js → Performance and routing
Framer Motion → UI transitions and interaction
GSAP → Advanced scroll and cinematic motion
Key Principles
Scene-based layout (not traditional sections)
Lazy-loaded media for performance
Scroll-triggered animations
Non-linear navigation
Experience Philosophy

This product is built around a simple idea:

Show, don’t explain.

Images = proof
Motion = emotion
UI = guidance

The result is an experience that feels closer to a film or product showcase than a website.

Final Note

This project is designed to set a new benchmark for commercial real estate storytelling.

If executed well, it shouldn’t just inform—it should make the viewer think:

“We need to be here.”